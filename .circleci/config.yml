version: 2.1

jobs:
  test:
    docker:
      - image: ramongarver/muii-ccfi:latest
    steps:
      - checkout
      - run:
          command: pnpm install-test

workflows:
  full-test:
    jobs:
      - test
